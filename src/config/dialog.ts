export enum Dialogs {
  ERROR_MESSAGE = 'error-message',
  LOGOUT = 'logout',
  CANCEL_APPLICATION = 'cancel-application',
  ORGANIZATION_CANCEL_CREATION_CONFIRMATION = 'organization-cancel-creation-confirmation',
  ORGANIZATION_SENT_NOTICE = 'organization-sent-notice'
}
